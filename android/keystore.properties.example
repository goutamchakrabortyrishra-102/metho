# Copy this file to android/keystore.properties and fill in the real values.
# android/keystore.properties is gitignored and must NEVER be committed.
# Keep the keystore outside the repo. PKCS12 keystores use the same value for both passwords.
storeFile=C:/Users/pc/Desktop/metho-release-key-2026.jks
storePassword=
keyAlias=metho
keyPassword=
